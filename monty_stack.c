#include "monty.h"

/**
* monty_stack - this is the main function dat sets d
* format of d data 2 a stack (LIFO)
* dis is d default behavior of d program
* @headboy: this variable os the head of the stack
* @countline: this is the variable line count
* Return: absolutely nothing (void)
*/
void monty_stack(stack_t **headboy, unsigned int countline)
{
	(void)headboy;
	(void)countline;
	func.fl = 0;
}
