#include "monty.h"

/**
* monty_nop - this main funct that does not do anything
* @headboy: this variable is the pointer to d head pointer
* @countline: this variable is the line count
* Return: absolutely nothing (void)
*/
void monty_nop(stack_t **headboy, unsigned int countline)
{
	(void) countline;
	(void) headboy;
}
